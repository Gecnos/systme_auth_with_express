{
    "configurations": [
        {"type":"node","request":"launch","name":"Run prisma","runtimeExecutable":"prisma","cwd":"${workspaceFolder}","args":[]},
        {
            "type": "pwa-node",
            "request": "launch",
            "name": "Launch Express Server (dev)",
            "program": "${workspaceFolder}/src/index.js",
            "runtimeArgs": [
                "--watch"
            ],
            "cwd": "${workspaceFolder}",
            "envFile": "${workspaceFolder}/.env",
            "console": "integratedTerminal",
            "internalConsoleOptions": "neverOpen"
        }
    ]
}